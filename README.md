# peer-play
Decentralized P2P Video Streaming Platform with Blockchain

## Setup

See [`P2P App/SETUP.md`](P2P%20App/SETUP.md) for the full run sequence - installing
the Hardhat contract environment, deploying the contract, recreating the git-ignored
config files (`app/config.js`, `app/schema.sql`), and starting the backend and client.

The attack-scenario guides live in [`docs/attacks/`](docs/attacks/).
